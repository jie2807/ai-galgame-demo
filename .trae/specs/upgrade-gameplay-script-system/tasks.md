# 剧本游玩功能改造升级 - 任务列表

## 任务依赖关系
- Task 1（UI重构）无依赖，可并行开始
- Task 2（剧本导入导出）依赖 Task 1 的部分UI改动（添加导入导出按钮）
- Task 3（对话流程修复）无依赖，可并行开始
- Task 4（测试验证）依赖 Task 1、2、3 全部完成

## 任务列表

- [ ] Task 1: NPC状态栏和世界状态栏UI重构
  - **目标**: 重新设计游戏页面顶部UI，好感度移至NPC头像下方，世界状态信息整合到顶部
  - **步骤**:
    - [ ] 1.1 修改HTML结构：调整 `game-npc-bar` 内部布局，移除好感度条，简化信息
    - [ ] 1.2 修改HTML结构：在 `npc-avatar-small` 下方添加好感度条容器 `npc-affection-bar`
    - [ ] 1.3 修改CSS样式：更新 `game-npc-bar` 的flex布局，适配新信息结构
    - [ ] 1.4 修改CSS样式：添加 `npc-affection-bar` 样式（宽度约40px，高度4px，圆角）
    - [ ] 1.5 修改CSS样式：根据好感度等级添加不同颜色类（.affection-green, .affection-blue, .affection-gold, .affection-purple）
    - [ ] 1.6 修改CSS样式：重新设计 `game-status-row`，确保四项信息（地址/天气/时间/健康）水平排列
    - [ ] 1.7 修改JS逻辑：`updateNPCBar()` 函数更新好感度条显示
    - [ ] 1.8 修改JS逻辑：`updateGameStateUI()` 函数确保地址信息在状态栏显示
    - [ ] 1.9 验证：页面加载后UI正确显示，好感度条位置和样式正确
  - **验证**: 浏览器中检查UI布局，好感度条在NPC头像下方，顶部显示地址/天气/时间/健康四项信息

- [ ] Task 2: 剧本导入导出系统实现
  - **目标**: 实现剧本JSON格式导出和导入功能，支持玩家分享剧本
  - **步骤**:
    - [ ] 2.1 在自定义剧本编辑页面添加"导出剧本"按钮HTML
    - [ ] 2.2 在首页或菜单添加"导入剧本"按钮HTML
    - [ ] 2.3 实现 `exportScript()` 函数：从当前配置生成标准JSON格式剧本数据
    - [ ] 2.4 实现 `downloadScriptFile(data, filename)` 函数：创建Blob并触发浏览器下载
    - [ ] 2.5 实现 `importScriptFile(file)` 函数：读取JSON文件，验证格式，加载到游戏
    - [ ] 2.6 实现 `validateScriptFormat(data)` 函数：检查版本、必需字段、数据完整性
    - [ ] 2.7 实现 `loadImportedScript(data)` 函数：将验证后的剧本数据加载到chapter 2
    - [ ] 2.8 添加导入导出按钮的CSS样式
    - [ ] 2.9 添加导入成功/失败的提示逻辑
    - [ ] 2.10 验证：导出剧本生成正确JSON，导入剧本能正确加载到游戏
  - **验证**: 导出剧本文件 → 检查JSON格式 → 导入剧本文件 → 验证剧本可正常游玩

- [ ] Task 3: 对话流程Bug修复与优化
  - **目标**: 修复对话系统中的已知bug，提升稳定性
  - **步骤**:
    - [ ] 3.1 修复消息恢复逻辑：`restoreMessages()` 时同步恢复NPC状态和好感度
    - [ ] 3.2 修复NPC切换流程：确保 `switchNPC()` 更新所有相关状态（上下文、UI、Live2D）
    - [ ] 3.3 修复流式响应中断：增强 `sendMessage()` 的错误处理，确保超时/abort后重置 `isStreaming`
    - [ ] 3.4 修复动作标签解析：`parseActionTagAndDisplay()` 增加try-catch，解析失败不影响文本显示
    - [ ] 3.5 优化地点变更检测：`detectArrival()` 增加模糊匹配和同义词识别
    - [ ] 3.6 修复好感度更新：确保AI回复后好感度正确计算和保存
    - [ ] 3.7 修复世界状态同步：地点变更后天气/时间/健康状态正确更新
    - [ ] 3.8 增强消息内容验证：防止空消息或无效消息发送到AI
    - [ ] 3.9 添加对话错误提示：网络错误/API错误时显示友好提示
    - [ ] 3.10 验证：所有修复点在实际游玩中正常工作
  - **验证**: 测试消息恢复、NPC切换、流式中断、动作标签、地点变更、好感度更新等场景

- [ ] Task 4: 集成测试与验证
  - **目标**: 全面测试所有改动，确保功能正常工作
  - **步骤**:
    - [ ] 4.1 测试UI布局：好感度条位置、顶部状态栏、响应式布局
    - [ ] 4.2 测试导出功能：生成JSON → 验证格式 → 下载文件
    - [ ] 4.3 测试导入功能：选择文件 → 验证格式 → 加载剧本 → 开始游玩
    - [ ] 4.4 测试对话流程：发送消息 → AI回复 → 动作标签 → NPC切换 → 地点变更
    - [ ] 4.5 测试异常场景：网络断开、API错误、无效JSON导入、消息恢复
    - [ ] 4.6 测试Live2D集成：模型加载、动作标签触发、切换NPC时模型更新
    - [ ] 4.7 修复发现的问题
  - **验证**: 所有测试用例通过，无严重bug

# Task Dependencies
- Task 1 无依赖
- Task 2 无依赖
- Task 3 无依赖
- Task 4 依赖 Task 1、Task 2、Task 3
