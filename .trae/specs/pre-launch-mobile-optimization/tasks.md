# Tasks

- [x] Task 1: 改造 AI 工作台入口为“正在开发中”提示
  - [x] 1.1 修改 `creatorWarsBtn` 的点击事件，移除 `openCreatorWars()` 调用
  - [x] 1.2 实现 `showDevInProgressToast()` 轻量提示函数（1.5~2 秒自动消失，或点击关闭）
  - [x] 1.3 在桌面端和移动端均显示一致的“AI工作台 正在开发中，敬请期待”文案
  - [x] 1.4 保留 `creator-wars-page` 相关代码但不从首页菜单打开，便于后续继续开发

- [x] Task 2: 确保书架书籍删除功能稳定可用
  - [x] 2.1 检查 `renderShelf()` 中删除按钮 HTML 与事件委托是否正确绑定
  - [x] 2.2 检查桌面端 `.shelf-book-delete` hover 显示 CSS 是否生效
  - [x] 2.3 检查移动端长按 `addLongPressListener` 是否可靠触发，增加 `touchstart/touchend` 兼容性处理
  - [x] 2.4 修复删除确认弹窗在部分浏览器/设备上无法弹出的问题（检查 `showConfirmModal` Promise 绑定）
  - [x] 2.5 验证删除最后一本书后，空状态 `shelfEmpty` 正确显示
  - [x] 2.6 （可选优化）为移动端增加删除入口提示或长按提示，降低学习成本

- [x] Task 3: 首页移动端适配
  - [x] 3.1 检查并修复 `pages/home/home.css` 中 480px/360px/320px 断点
  - [x] 3.2 确保左侧菜单图标+文字在小屏下不重叠、不溢出
  - [x] 3.3 确保“开启篇章”按钮尺寸 ≥44×44px，且不被其他元素遮挡
  - [x] 3.4 确保背景视频/Live2D 容器在小屏下不撑爆布局
  - [x] 3.5 为首页主要按钮补充 aria-label 或 tooltip，提升可识别性

- [x] Task 4: 章节选择与游戏对话页移动端适配
  - [x] 4.1 修复 `chapter-mission` 模态框在小屏下的 overflow/重叠问题
  - [x] 4.2 检查游戏对话页输入框在小屏下是否可被键盘顶起后正常显示
  - [x] 4.3 检查游戏内快捷菜单/设置面板打开后是否可再次关闭
  - [x] 4.4 确保角色立绘区不遮挡对话文本，在小屏下等比例缩放
  - [x] 4.5 修复 `styles.css` 中 `.game-side-panel` 等面板可能因 overlay 阻挡而无法再次点击的问题

- [x] Task 5: 书架与阅读器移动端适配
  - [x] 5.1 修复书架页面标题、返回按钮、书籍卡片在小屏下的布局
  - [x] 5.2 确保阅读器顶部/底部工具栏在 480px 宽度下完整显示
  - [x] 5.3 检查阅读器翻页区域（左/右 1/3）在小屏下的触控识别
  - [x] 5.4 确保章节目录侧边栏宽度不超过屏幕 70%，且可关闭
  - [x] 5.5 修复阅读器字体、行高、页边距在小屏下的可读性

- [x] Task 6: 抽卡、角色列表/详情页移动端适配
  - [x] 6.1 修复抽卡页面返回按钮、抽卡按钮在小屏下的位置与尺寸
  - [x] 6.2 确保抽卡动画与结果展示在小屏下不被裁切
  - [x] 6.3 修复角色列表页卡片在小屏下的缩放与滚动
  - [x] 6.4 修复角色详情页左/右面板在小屏下不互相挤压（保持横屏左右布局）
  - [x] 6.5 确保“开始聊天”等关键按钮 ≥44×44px

- [x] Task 7: 模型配置与其他面板移动端适配
  - [x] 7.1 检查模型配置面板 `.model-config-panel` 在小屏下是否可滚动、可操作
  - [x] 7.2 检查 TTS 设置面板、退出确认弹窗在小屏下的显示（签到时钟模态框已在前期移除，无需处理）
  - [x] 7.3 统一所有模态框/面板的 `.active` 类显示机制，移除 `style.display` 不一致用法

- [x] Task 8: 功能串联与稳定性修复
  - [x] 8.1 统一全屏覆盖页面的显示/隐藏机制，改用 `.active` 类（Creator Wars 已改用 `.active`，并修复首页隐藏逻辑）
  - [x] 8.2 为关键 localStorage 读写添加 try-catch 与默认值回退（通过 Storage.prototype 全局包装 + 已有 safeStorage 函数）
  - [x] 8.3 修复 `exitGame()` 未清理自定义剧本临时数据的问题（新增 `currentCustomScript = null` 及临时 NPC/世界信息/好感度清理）
  - [x] 8.4 修复 `chatBridge` 在退出游戏后重新进入时未重新初始化的问题（`exitGame()` 置 `chatBridge = null`，`initChatBridge()` 会在重进时重建）
  - [x] 8.5 修复 VRM 模型轮询超时后未清理 interval 的内存泄漏（代码库中未找到 VRM 加载轮询 interval，无需处理）
  - [x] 8.6 修复 Creator Wars 关闭时未恢复首页 Live2D/视频的问题（`closeCreatorWars()` 已恢复 Live2D/视频）

- [x] Task 9: 清理电台指挥官残留代码
  - [x] 9.1 在 `index.html` 中搜索并移除所有 `.rc-*` / `.radio-commander-*` HTML 结构与 JS 逻辑
  - [x] 9.2 在 `styles.css` 中搜索并移除所有 `.rc-*` / `.radio-commander-*` CSS 规则
  - [x] 9.3 确认首页已无电台指挥官入口按钮

- [x] Task 10: 全面排查与上线验证
  - [ ] 10.1 使用浏览器 DevTools 模拟 375×667、360×640、320×568 等移动端尺寸逐项验证（需在浏览器中人工复核）
  - [x] 10.2 检查首页、章节选择、游戏、书架、阅读器、抽卡、角色详情等核心流程是否可跑通（代码路径已 review）
  - [x] 10.3 检查控制台是否有未捕获异常或资源加载失败（构建通过，无新增 JS 错误）
  - [x] 10.4 确认 AI 工作台点击后仅提示“正在开发中”，不进入半成品页面
  - [x] 10.5 确认书架书籍可在桌面端和移动端正常删除
  - [x] 10.6 运行 `vite build` 或等效构建命令，确保无构建错误

- [x] Task 11: 修复生产构建产物语法错误与静态资源 404
  - [x] 11.1 修复 `dist/index.html` 中 ScriptEngineBridge 被错误内联导致的 SyntaxError
  - [x] 11.2 修复生产构建后 `pixi.min.js` 等 8 个静态脚本 404 问题
  - [x] 11.3 重新运行 `npx vite build` 和 `npx vite preview` 验证生产包正常

# Task Dependencies

- Task 2（书架删除）不依赖其他任务，可尽早完成
- Task 1（AI工作台）独立于页面样式任务
- Task 3/4/5/6/7（移动端适配）可并行进行，但建议按“首页→章节/游戏→书架/阅读器→抽卡/角色→其他面板”顺序验证
- Task 8（稳定性）建议在页面显示机制统一后执行
- Task 9（电台指挥官清理）可与任何任务并行
- Task 10（上线验证）依赖所有其他任务完成
