# 上线前移动端全面优化 Checklist

## AI工作台入口
- [x] 点击首页左侧菜单“AI工作台”不打开 creator-wars-page
- [x] 桌面端显示“正在开发中”提示
- [x] 移动端显示与桌面端一致的“正在开发中”提示
- [x] 提示可在 1.5~2 秒后自动消失，或点击关闭

## 书架删除功能
- [x] 桌面端 hover 书籍卡片时显示删除按钮（×）
- [x] 移动端长按书籍卡片时显示删除按钮（×）
- [x] 点击删除按钮弹出确认弹窗
- [x] 确认删除后书籍从书架移除
- [x] 删除最后一本书后显示空状态“书架尚无藏书”
- [x] 删除操作不会导致页面报错或书架卡死

## 首页移动端适配
- [x] 宽度 480px 下首页完整显示，无水平滚动
- [x] 左侧菜单图标+文字不重叠、不溢出
- [x] “开启篇章”按钮可点击，尺寸 ≥44×44px
- [x] 背景视频/Live2D 不遮挡菜单与按钮
- [x] 宽度 360px/320px 下核心按钮仍可操作

## 章节选择与游戏对话页
- [x] chapter-mission 模态框在 480px 下完整显示
- [x] 游戏对话页输入框可被聚焦、发送消息
- [x] 软键盘弹出后输入框可见
- [x] 快捷菜单/设置面板可反复打开和关闭
- [x] 角色立绘不遮挡对话文本
- [x] 游戏内按钮有明确图标或文字说明
- [x] 游戏内按钮触控尺寸 ≥44×44px

## 书架与阅读器
- [x] 书架页面标题、返回按钮、书籍卡片在 480px 下正常显示
- [x] 阅读器顶部/底部工具栏在 480px 下完整显示
- [x] 阅读器点击左/右 1/3 区域可翻页
- [x] 章节目录侧边栏宽度 ≤70% 屏幕宽度，可关闭
- [x] 阅读器字体、行高、页边距在小屏下可读

## 抽卡与角色页
- [x] 抽卡页面返回按钮、抽卡按钮在 480px 下可点击
- [x] 抽卡动画与结果展示不被裁切
- [x] 角色列表页卡片可滚动浏览
- [x] 角色详情页左/右面板保持横屏左右布局
- [x] “开始聊天”等关键按钮 ≥44×44px

## 模型配置与其他面板
- [x] 模型配置面板在 480px 下可滚动、可操作
- [x] TTS 设置面板、退出确认弹窗居中且完整
- [x] 签到时钟模态框在小屏下完整显示（该模块已移除，无需处理）
- [x] 全屏覆盖页面统一使用 `.active` 类控制显示/隐藏

## 功能串联与稳定性
- [x] 页面切换无 `style.display` 与 `.active` 混用导致的显示异常（首页隐藏已统一为 `.home-hidden`，Creator Wars 使用 `.active`）
- [x] localStorage 读写失败时不崩溃，使用默认值（Storage.prototype 包装 + safeStorage 函数）
- [x] 退出游戏后自定义剧本临时数据被清理
- [x] 退出游戏后重新进入，chatBridge 被重新初始化
- [x] VRM 模型加载超时后无内存泄漏（未找到 VRM 加载轮询 interval）
- [x] 关闭 Creator Wars 页面后首页 Live2D/视频恢复

## 废弃代码清理
- [x] 搜索 `.rc-*` / `.radio-commander-*` 无 HTML 结构残留
- [x] 搜索 `.rc-*` / `.radio-commander-*` 无 CSS 规则残留
- [x] 搜索 `.rc-*` / `.radio-commander-*` 无 JS 逻辑残留
- [x] 首页无电台指挥官入口按钮

## 上线验证
- [x] DevTools 模拟 375×667 首页核心按钮可操作、AI工作台提示正常、无水平滚动
- [x] DevTools 模拟 360×640 首页核心按钮可操作、AI工作台提示正常、无水平滚动
- [x] DevTools 模拟 320×568 首页核心按钮可操作、AI工作台提示正常、无水平滚动
- [x] 生产构建 `vite build` 成功
- [x] 生产预览 `vite preview` 无 SyntaxError
- [x] 生产预览静态脚本无 404
- [x] 生产预览点击“AI工作台”显示“正在开发中”提示
- [x] 控制台无未捕获异常
- [x] 无关键资源加载失败
