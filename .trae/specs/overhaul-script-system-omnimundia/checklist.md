# Checklist

## 数据模型与版本迁移
- [ ] v2.0 剧本数据模型 Schema 定义完成，包含 metadata/worldSettings/characters/items/scriptConfig/events
- [ ] `validateScriptData()` 验证函数正确检查必需字段和类型
- [ ] `migrateScriptData()` 支持 v1.0 → v2.0 自动迁移，新字段填充默认值
- [ ] `getCustomConfig()` 和 `saveCustomConfig()` 使用新数据模型
- [ ] `buildExportScriptData()` 生成 v2.0 格式导出文件

## 世界树存档分支系统
- [ ] 世界树数据结构设计完成（树节点含 parentId、children、saveData）
- [ ] `WorldTreeStorage` 存储引擎基于 localStorage 正确存取
- [ ] 从任意存档节点创建分支，新分支与原存档独立
- [ ] 世界线切换正确恢复完整状态（对话/世界状态/好感度/物品/属性）
- [ ] 自动存档每 5 轮触发，同一世界线保留最近 10 个自动存档
- [ ] 世界树可视化 UI 正确显示树状结构，当前线高亮，节点可点击
- [ ] 原有 5 槽位存档系统已被世界树系统替代

## 剧本发现与分类系统
- [ ] 标签系统可用：预设标签 + 自定义标签，每个剧本最多 8 个
- [ ] 剧本卡片 UI 显示封面图、标题、作者、简介、标签、NPC 数量
- [ ] 封面图功能可用：上传/选择封面图，未设置时显示默认封面
- [ ] 标签筛选栏和排序选项（最近游玩/创建时间/名称）正常工作
- [ ] 内置剧本和自定义剧本统一展示
- [ ] 内置剧本已添加标签和元数据

## 剧本创作自由度提升
- [ ] NPC 数量上限已从 3 提升到 10，编辑器支持滚动
- [ ] 自定义物品库编辑器可用（名称/描述/类型/图标/效果）
- [ ] 叙事视角选择可用（第一人称/第三人称），System Prompt 正确调整
- [ ] 内置骰子系统可用（d4/d6/d8/d10/d12/d20/d100），UI 按钮和结果显示正确
- [ ] 剧本编辑功能可用：创建后可修改，编辑器加载已有剧本配置
- [ ] 不确定性等级设置可用（低/中/高），影响 System Prompt

## 动态世界事件引擎
- [ ] 事件数据结构定义完成（conditions/actions/once/priority/cooldown）
- [ ] 事件条件检查器支持所有 8 种条件类型
- [ ] 事件动作执行器支持所有 8 种动作类型
- [ ] 事件触发流程正确：AI 回复后检查 → 优先级排序 → 最多触发 2 个
- [ ] 一次性事件触发后标记为已触发，不重复触发
- [ ] 事件编辑器 UI 可用（条件组合、动作配置、一次性/冷却设置）
- [ ] 事件引擎已集成到 SimpleChatEngine 的回复处理流程

## 对话引擎智能化
- [ ] 对话摘要系统：超过 30 条消息自动摘要，保留关键信息
- [ ] 语义 Lorebook 匹配：contextTags 上下文感知，优先级权重排序，token 预算控制
- [ ] 智能时间推进：解析 AI 回复中的时间描述，语义化推进时间
- [ ] 增强地点检测：别名匹配、上下文推断、地点变更时天气更新
- [ ] `_buildSystemPrompt()` 使用模块化构建

## 剧本导入导出增强
- [ ] 导出支持 v2.0 完整数据模型（含封面图 base64、物品库、事件）
- [ ] 导入支持 v1.0 和 v2.0 格式自动识别和迁移
- [ ] 导入预览卡片显示封面图、标题、作者、简介、标签、NPC 数量
- [ ] `validateScriptFormat()` 支持 v2.0 验证规则

## UI 升级
- [ ] 剧本编辑器包含物品库编辑区
- [ ] 剧本编辑器包含事件编辑区
- [ ] 剧本编辑器包含叙事视角选择、骰子开关、不确定性等级
- [ ] 剧本编辑器包含封面图上传
- [ ] 剧本列表包含标签筛选栏和排序选项
- [ ] 世界树存档可视化 UI 正常工作
- [ ] 骰子 UI 正常工作（输入框旁骰子按钮、结果展示）

## 回归测试
- [ ] 内置剧本（紫罗兰永恒花园）正常游玩
- [ ] 自定义剧本创建、保存、加载、游玩正常
- [ ] 好感度系统正常工作
- [ ] Live2D 模型加载和切换正常
- [ ] 物品系统（获取/使用/赠送）正常
- [ ] 世界状态（地点/天气/时间/健康）正常更新
- [ ] 导入导出功能正常
